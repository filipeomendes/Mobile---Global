<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlobalSolution"
        tools:targetApi="31">

        <activity android:name=".activity.LoginActivity"
            android:label="Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MenuActivity"
            android:label="Menu"
            android:exported="true" />

        <activity android:name=".activity.CadastroProdutoActivity"
            android:label="Cadastro de Produtos"
            android:exported="true" />

        <activity android:name=".activity.CadastroClienteActivity"
            android:label="Cadastro de Clientes"
            android:exported="true" />

        <activity android:name=".activity.ListaProdutoActivity"
            android:label="Lista de Produtos"
            android:exported="true" />

        <activity android:name=".activity.ListaClienteActivity"
            android:label="Lista de Clientes"
            android:exported="true" />

        <activity android:name=".activity.AlterarProdutoActivity"
            android:label="Alterar Produto"
            android:exported="true" />

        <activity android:name=".activity.AlterarClienteActivity"
            android:label="Alterar Cliente"
            android:exported="true" />

        <activity android:name=".activity.SuporteActivity"
            android:label="Suporte"
            android:exported="true" />


    </application>

</manifest>